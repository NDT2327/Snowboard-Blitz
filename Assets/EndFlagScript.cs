using UnityEngine;
using UnityEngine.SceneManagement; // Để load màn mới

public class EndFlagScript : MonoBehaviour
{
    //private void OnCollisionEnter2D(Collision2D collision)
    //{
    //    if (collision.gameObject.CompareTag("Player")) // Kiểm tra nếu nhân vật chạm vào
    //    {
    //        LoadNextLevel();
    //    }
    //}

    //void LoadNextLevel()
    //{
    //    int currentSceneIndex = SceneManager.GetActiveScene().buildIndex; // Lấy màn hiện tại
    //    SceneManager.LoadScene(currentSceneIndex + 1); // Load màn tiếp theo
    //}
}
